#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")/.."

if ! bundle exec foreman version >/dev/null 2>&1; then
  echo "Foreman is not installed. Run \`bundle install\` to install project gems." >&2
  exit 1
fi

exec bundle exec foreman start -f Procfile.dev "$@"
